[project]
name = "galenus-cts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "lxml>=6.0.4",
]

[dependency-groups]
dev = [
    "ruff>=0.15.10",
]
